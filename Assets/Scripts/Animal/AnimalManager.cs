using Graph;
using UnityEngine;
using Visual;

namespace Animal

{
    public class AnimalManager : MonoBehaviour
    {
        [SerializeField]
        private VisualRoot _visualRoot;
        [SerializeField]
        private GraphSingleton _graphSingleton;

        void Start()
        {

        }
    }
}