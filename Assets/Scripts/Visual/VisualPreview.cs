using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;


namespace Visual
{ /*
    [System.Serializable]
    public class Parameter {
        public Interactions.InterActionType type;
        public float amount;
    } 

    [UnityEditor.CustomEditor(typeof(VisualizationLibrary), true)]
    public class VisualPreview : UnityEditor.Editor
    {
        public Editor gameObjectEditor;
        public List<Parameter> parameters = new List<Parameter>();      

        [MenuItem("Kotikivi/Preview Visualization")]
        public static void ShowWindow()
        {
            EditorWindow.GetWindow(typeof(VisualPreview));
        }

        public override bool HasPreviewGUI() { return true; }


        public override void OnPreviewGUI(Rect r, GUIStyle background)
        {
            parameters.ForEach(param => { param.amount = EditorGUILayout.Slider(param.type.ToString(), param.amount, 0, 1); });
   
            
            
        }

    }
*/
}